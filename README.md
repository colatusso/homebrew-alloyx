# Homebrew tap for AlloyX

```bash
brew install colatusso/alloyx/allx
```

Installs the [AlloyX](https://github.com/colatusso/alloyx) CLI (`allx`) — run
your Salesforce Apex locally on the JVM — together with the JDK it needs.
